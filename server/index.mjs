import server from './Server.js';

const Server=server.Server;
const Config=server.Config;
const RefString=server.RefString;

export {
    Config,
    RefString,
    Server
};

export default server;
